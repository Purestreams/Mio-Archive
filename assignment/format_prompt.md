Reformat this assignment answer file to match the style used in `stat4608As1.md`.

Requirements:

1. Add YAML frontmatter in this format:

```yaml
---
title: "[Course Code] Assignment [Number]"
author: "Zhu Yecheng 3036061373"
date: "[YYYY-MM-DD]"
geometry: margin=2cm
---
```

2. Use section headings in this style:

```md
## Q1
### (a)
### (b)
```

3. Preserve the original answers, logic, and calculations. Do not change the meaning unless fixing an obvious formatting issue.

4. Format mathematical expressions cleanly:

- Use inline math for short expressions.
- Use display math blocks for key formulas, derivations, and final calculations.
- Put important equations on their own lines using `$$ ... $$`.

5. Match the writing style of `stat4608As1.md`:

- Clear spacing between paragraphs and equations.
- Direct academic tone.
- Short explanatory lead-ins before calculations where useful.
- Consistent heading hierarchy.

6. For short-answer questions:

- Start with the direct answer, such as `False.` or `**5 GHz band**.`
- Follow with a concise explanation in paragraph form.
- Use numbered points only when the original content is naturally list-based.

7. Clean up markdown presentation:

- Remove decorative separators unless needed.
- Avoid mixed bullet formatting on a single line.
- Keep spacing consistent throughout.

8. Output only the fully reformatted markdown.